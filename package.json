{
  "name": "elo-rating-system",
  "version": "1.1.2",
  "description": "elo calculator in javascript",
  "author": "Matthew D Webb <matt.d.webb@icloud.com>",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest --coverage",
    "start": "tsc && node dist/index.js",
    "build": "tsc",
    "view:coverage": "serve coverage/lcov-report",
    "clean": "rm -rf coverage src/**/*.js src/**/*.map"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}
